service: ozisanat-api
frameworkVersion: "3"

provider:
  name: aws
  memorySize: 2048
  stage: prod
  timeout: 15
  region: eu-central-1
  runtime: nodejs12.x

functions:
  hello:
    handler: handler.hello
    events:
      - http: ANY /{proxy+}
      - http: ANY /
